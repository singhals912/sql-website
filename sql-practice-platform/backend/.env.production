# Production Environment Variables for Railway
# Replace these values with your actual Railway database URLs

NODE_ENV=production
PORT=5001

# Frontend URL - will be updated once domain is connected
FRONTEND_URL=https://datasql.pro

# Security - Railway will provide these
# JWT_SECRET will be set in Railway dashboard
# DATABASE_URL will be auto-provided by Railway PostgreSQL
# REDIS_URL will be auto-provided by Railway Redis

# Rate Limiting (production values)
RATE_LIMIT_WINDOW=15
RATE_LIMIT_MAX_REQUESTS=1000

# Features
HEALTH_CHECK_ENABLED=true
METRICS_ENABLED=true